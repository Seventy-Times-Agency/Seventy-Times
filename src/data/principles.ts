export type Principle = {
  num: string;
  title: string;
  body: string;
};

// Principles live inline per language via the dictionary — this file
// exists only for type sharing. Real content is in src/i18n/dictionary.ts.
